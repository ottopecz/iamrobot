{
  "name": "robby",
  "version": "1.0.0",
  "description": "robby the robot",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "script": {
    "start": "npx pm2 server.js"
  },
  "dependencies": {
    "pm2": "^3.0.3"
  }
}
